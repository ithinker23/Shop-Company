import React from 'react';
import ReactDOM from 'react-dom/client';
import LoginPage from './LoginPage';

const root = ReactDOM.createRoot(document.getElementById('loginPageRoot'));
root.render(
  <React.StrictMode>
    <LoginPage />
  </React.StrictMode>
);
